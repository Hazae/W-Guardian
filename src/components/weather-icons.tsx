import styled from "styled-components";
import { WeatherProps } from "../type/types";

export const WeatherIcons: React.FC<WeatherProps> = () => {
  return <IconsCon></IconsCon>;
};

const IconsCon = styled.div``;
